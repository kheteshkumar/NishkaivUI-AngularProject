export enum RelationEnum {
  Self = '0',
  Spouse = '1',
  Parent = '2',
  Other = '3',
}

export enum InsuranceType {
  Primary = 1,
  Secondary = 2,
  Other = 3
}

export enum InsuranceTypeMapEnum {
  'Primary' = 'Primary',
  'Secondary' = 'Secondary',
  'Other' = 'Other'
}